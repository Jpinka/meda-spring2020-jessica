//propertys
// PI is a variable belonging to the Math library
Math.PI;
Math.SQRT2;

//Mathod
Math.random(); 


function random() {
}
random();

//How add, substract, and clear arrays.

// Array.push() method. arguement provided is added to the end of the array.

// Array.pop(). Takes no argument. removes and RETURNS the last item in the array

var removeDataPiece = myArray.pop();

console.log(myArray);
console.log(removedDataPiece);

// Array.shift() me4thod. no arguemnt, removes and RETURNS the first item in the array.

var removedFirstItem = myArray.shift();

console.log(myArray);
console.log(removedFirsatItem);

// Array.unshift() method, arguemnt provided is added to the start of the array

myArray.unshift(1000);
console.log(myArray);

// Array length property. Returns the amount of items in the array in the form of a number.

console.log(myArray.length);

// Example=playtime 

